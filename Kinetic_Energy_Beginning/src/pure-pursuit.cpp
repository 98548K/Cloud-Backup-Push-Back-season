#include "vex.h"

/*const double lookahedDistance = 50;
void setPath(double pathX, double pathY) {
    std::list<double> path = {};
    double itr. = 0;
    while (path != true) {
        path[itr.][1] += {pathX};
        path[itr.][2] += {pathY};
        itr. += 1;
    }
}

int len(double param[]) {
    int length = 0;
    while (param[length + 1] != true) {
        length += 1;
    }
    return length;
}

void purePursuitPath(double desiredValue[]) {
    while (true) {
        
    }
}
void append(double listParam, double listValue) {
    listParam.push_back(listValue);
    
}*/